import FormButtons from './formButtons'
import FormInputArea from './formInputArea'
import FormRoot from './formRoot'

export const Form = {
  Root: FormRoot,
  Buttons: FormButtons,
  InputArea: FormInputArea,
}
